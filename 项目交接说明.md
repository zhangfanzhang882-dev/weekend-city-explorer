# 周末城市探索指南 — 项目交接说明

> 用途：切换新对话窗口后，凭本文件即可恢复全部上下文。
> 最后更新：2026-09-15 22:25（本地时间）
> 项目目录：`/Users/bytedance/Desktop/旅行`

---

## 一、需求背景

### 题目与场景
**周末城市探索指南**。大学生周末常纠结"去哪玩"——展览、市集、演出、短途徒步等活动信息分散，且受天气、预算、同行人数影响大。要求用 AI Coding 工具设计一个应用，根据用户偏好、天气、预算推荐本地活动，支持组队出发、打卡记录、攻略分享。

### 提交要求（硬约束，来自面试方）
| 要求 | 说明 |
|---|---|
| 交付形式 | **仅提交一个可在线访问的部署链接**，必须真实部署，不能只本地运行 |
| 域名稳定 | **域名必须保持不变**，后续在同域名下持续迭代。域名变更则面试官无法打开 |
| 访问权限 | 必须为"公开"且可长期访问，不能因权限或时效设置导致打不开 |
| 时限 | 30 分钟内提交一个可访问的雏形链接即可 |

### 关键决策：为什么用个人 GitHub + Cloudflare Pages
提出的顾虑是：**企业版豆包生成的链接在离职后可能无法长期访问**，而面试官需要长期打开。
因此确定方案为 **个人 GitHub 仓库 + Cloudflare Pages**，与企业账号完全解绑，离职后不受影响。前端为纯静态构建产物，长期免费托管，域名固定。

### 用户交互路径（确定的 9 步）
1. 用户进入后获取地址权限，支持切换地址（有默认值）
2. 用户选择想去哪里玩，默认当前地址
3. 勾选时间
4. 捕捉时间范围内的天气
5. 检索小红书/高德/美团等获取景点数据
6. 基于上述内容分别生成**预览版本**的旅游线路
7. 支持在 app 内发起拼车、邀请同行
8. 行程确定后，单个旅行卡片支持**打卡和分享**
9. 完整线路结束后支持**发布**；并要考虑"如果没按这个线路走该怎么办"

### 范围裁剪（已明确）
- 第 7 步的拼车/组队：**仅做同行邀请与费用约定的演示流程**，当前只是 demo，不需要做细致的真实拼车逻辑。
- 第 9 步的"没按线路走"：已通过"打卡/跳过/替换/补充临时地点"解决——最终发布的是**实际走过的路线**，不是原计划。

### 追加要求
- 参考过往同类项目的代码构成，**引入真实 API 数据**，并**希望引入一些 AI 能力**（不满足于纯 mock）。
- 在 VS Code 中本地部署代码，再推送到 GitHub。
- 要求本地改完代码后能把新代码发布上去（可持续迭代）。

---

## 二、当前进度

### 总体状态
**前端已公开上线可访问；AI 真实数据链路代码已完成并在本地验证通过，但线上生产环境的密钥尚未配置，因此线上 AI 生成功能暫不可用。**

### 固定公开地址
```
https://weekend-city-explorer.pages.dev
```
- GitHub 仓库：`zhangfanzhang882-dev/weekend-city-explorer`
- 生产分支：`main`｜构建命令：`npm run build`｜输出目录：`dist`
- 本地与远端已完全同步（无未提交改动，与 `origin/main` 零差异）

### 实测验证结果（2026-09-15 22:20 前后）
| 验证项 | 方式 | 结果 |
|---|---|---|
| 线上首页 | `curl -L` 线上地址 | ✅ HTTP 200 |
| 线上 AI 接口 | 真实 POST `/api/plan` | ❌ HTTP 503 `服务端尚未配置高德或 AI 密钥` |
| 本地 AI 接口 | 本地 Pages 服务真实 POST | ✅ HTTP 200，4.9 秒返回 |
| 本地链路数据 | 检查返回内容 | ✅ 真实天气 + 20 个真实 POI + 3 条 AI 路线 |
| TypeScript 类型检查 | `npm run typecheck` | ✅ 通过 |
| ESLint | `npm run lint:eslint` | ✅ 通过 |
| 生产构建 | `npm run build` | ✅ 通过（1882 模块，274ms） |

本地真实返回样例（节选，证明链路真实可用）：
- 天气：`2026-09-18 小雨 24–30℃`，来源"高德天气"
- 路线一"静安展览漫步线"：上海展览中心（评分 4.9）→ 上海市历史博物馆（4.7）→ 静安雕塑公园（4.7）
- 全部地点带真实地址、地铁出口信息与高德评分

### 交互路径实现情况
| 步骤 | 状态 | 实现说明 |
|---|---|---|
| 1 定位与切换地址 | ⚠️ 演示级 | `locate()` 为 650ms 延时后固定回填"上海/静安"，**未接入真实浏览器定位 API**；城市可手动输入 |
| 2 选择区域 | ✅ | 三个预设区域按钮：当前位置附近／静安／徐汇滨江 |
| 3 勾选时间 | ✅ | 自动算出本周六/周日两个按钮 |
| 4 获取天气 | ✅ 真实 | 高德天气 API，含"超出预报窗口"降级提示 |
| 5 检索景点 | ✅ 真实（仅高德） | 高德 POI 检索，5 组关键词，含去重与低质过滤。**小红书/美团未接入** |
| 6 生成预览路线 | ✅ 真实 AI | DeepSeek 生成 3 条差异化路线，服务端强制校验地点必须来自真实候选 |
| 7 邀请同行 | ✅ 演示级（按约定） | 弹窗含同行人数与费用约定，符合"仅做演示"的裁剪 |
| 8 打卡与分享 | ✅ | 每个卡片支持打卡／跳过／替换／分享 |
| 9 发布线路 | ✅ | 支持补充临时地点，发布"实际走过"的路线，含成功页 |

### 防编造机制（已实现，值得向面试官说明）
- AI 提示词强制"只从候选地点中选择，不得创造新地点或修改地点名称"
- 服务端用 `poiMap` 逐个校验 AI 返回的地点名，对不上的直接丢弃
- 有效路线少于 2 条则报错，不返回半成品
- 天气或地点查询失败时明确报错，**不用模拟数据顶替**
- 前端明确标注数据来源与"真实地点候选"数量

---

## 三、唯一剩余的阻塞项

### 问题
线上 `/api/plan` 返回 503，因为 Cloudflare 生产环境没有配置密钥。代码逻辑本身没问题（本地同一套代码跑通）。

### 阻塞原因
`npx wrangler whoami` 显示 **未登录**（`You are not authenticated`）。登录需要在浏览器中完成 Cloudflare 账号授权，**必须由你本人操作**，我无法代做。

### 解决办法（二选一，推荐 A）

**A. Cloudflare 网页控制台配置（推荐，不需要命令行登录）**
1. 打开 Cloudflare Dashboard → Workers & Pages → 选择 `weekend-city-explorer`
2. Settings → Environment variables → Production
3. 添加以下 4 个变量（值取自本地 `.dev.vars`）：
   - `AMAP_WEB_SERVICE_KEY`
   - `DEEPSEEK_API_KEY`
   - `DEEPSEEK_BASE_URL`（`https://api.deepseek.com`）
   - `DEEPSEEK_MODEL`（`deepseek-chat`）
   - 前两个建议用 **Secret（加密）** 类型
4. 保存后**重新触发一次部署**（环境变量对已完成的构建不生效）：Deployments → Retry deployment，或本地随便提交一次推送
5. 验证：
   ```bash
   curl -X POST "https://weekend-city-explorer.pages.dev/api/plan" \
     -H "content-type: application/json" \
     -d '{"city":"上海","area":"当前位置附近","date":"2026-09-19","budget":300,"interests":["展览","市集"],"partySize":2}'
   ```
   期望返回 200 且带 `routes` 字段。

**B. 命令行方式**
```bash
cd "/Users/bytedance/Desktop/旅行"
npx wrangler login          # 浏览器授权，需你本人完成
npx wrangler pages secret put AMAP_WEB_SERVICE_KEY --project-name weekend-city-explorer
npx wrangler pages secret put DEEPSEEK_API_KEY --project-name weekend-city-explorer
```

### 注意：密钥安全
`.dev.vars` 存有真实密钥，已在 `.gitignore` 中忽略，**不会进仓库**。仓库内只有不含真实值的 `.dev.vars.example` 模板。请勿把真实密钥写入任何被 git 跟踪的文件。

---

## 四、更改记录

### 提交历史
| 提交 | 时间 | 内容 |
|---|---|---|
| `704a228` | 09-15 20:33 | Initial weekend city explorer demo — 首版 demo，完整 9 步交互流程（mock 数据） |
| `35d0421` | 09-15 21:07 | Document VS Code and Cloudflare deployment workflow — 补充部署与本地迭代文档 |
| `aaa035c` | 09-15 21:51 | Add live Amap data and AI route generation — 接入高德真实数据与 DeepSeek AI 路线生成 |

### 最后一次提交（`aaa035c`）的具体改动
新增 `functions/api/plan.ts`（233 行，Cloudflare Functions 服务端）、`src/api/plan.ts`（前端调用封装）、`wrangler.toml`、`.dev.vars.example`；改造 `HomePage.tsx`（+92 行，从 mock 切到真实接口）、`JourneyPanel.tsx`；更新 `.gitignore`（忽略 `.dev.vars` 与 `.wrangler/`）与 `package.json`（新增 `dev:full`、`typecheck`、`lint` 脚本，引入 wrangler 依赖）。

### 本次会话（已提交后）的增量修正
均已包含在 `aaa035c` 中，无未提交改动：
1. **天气日期不匹配处理**：高德只提供近几日预报，所选周末可能尚未进入预报窗口。新增 `forecastStatus: 'exact' | 'out_of_range'` 与 `note` 字段，超窗时明确提示"尚未进入预报窗口，当前展示最远可用日期 X，路线按临近天气规划"，前端用 `warning` 主题色高亮，**不伪装成精确预报**。
2. **地点去重优化**：同一场馆的不同入口/子馆（如"东一馆""西馆""售票处"）曾被 AI 当成多个独立景点。新增 `canonicalName()` 归一化与 `majorVenuePatterns` 大型场馆合并，另加 `lowQualityPattern` 过滤菜市场、停车场、卫生间等低质结果。
3. **配置文件**：新增 `wrangler.toml`（`pages_build_output_dir = "./dist"`），`.gitignore` 补充 `.wrangler/`。

---

## 五、技术架构

### 技术栈
React 19 + TypeScript + Tailwind CSS v4 + shadcn/ui + lucide-react + react-router-dom；构建 Vite 8；部署 Cloudflare Pages + Pages Functions（Serverless，无需单独买后端服务器）。

### 关键文件
```
旅行/
├── functions/api/plan.ts      # 【核心】服务端：高德天气+POI+DeepSeek 编排、防编造校验
├── src/
│   ├── api/plan.ts            # 前端接口封装与类型定义
│   ├── pages/HomePage/        # 主页面：plan/routes/journey/published 四态机
│   ├── components/
│   │   ├── JourneyPanel.tsx   # 打卡/跳过/替换/分享/发布/邀请同行
│   │   └── RouteCard.tsx      # 路线卡片
│   ├── data/trips.ts          # IRoute 等类型定义
│   └── tailwind-theme.css     # 主题变量（含 warning 色）
├── .dev.vars                  # 【不进仓库】真实密钥
├── .dev.vars.example          # 密钥模板
├── wrangler.toml              # Cloudflare Pages 配置
└── README.md                  # 技术规范与部署流程
```

### 数据流
```
用户填写条件（城市/区域/日期/兴趣/预算）
   ↓ POST /api/plan
Cloudflare Functions 并行请求：
   ├── 高德 district → adcode → 天气预报（含超窗降级）
   └── 高德 POI 检索 ×5 关键词 → 去重 → 过滤 → 取 20 个
   ↓ 真实天气 + 真实候选地点
DeepSeek（temperature 0.35，json_object 模式）
   ↓ 3 条路线草稿（仅含地点名）
服务端用 poiMap 校验每个地点名必须真实存在
   ↓ 校验通过（不足 2 条则报错）
返回 { weather, routes, poiCount, sources }
```

### 常用命令
```bash
cd "/Users/bytedance/Desktop/旅行"
npm install
npm run dev                     # 纯前端开发（接口不可用）
npm run dev:full                # 【推荐】构建+本地 Pages 服务，接口可用，读 .dev.vars
npm run lint                    # typecheck + eslint
npm run build                   # 生产构建
git add . && git commit -m "..." && git push origin main   # 推送后 Cloudflare 自动部署
```

---

## 六、下一步建议

**必做（阻塞面试评审的核心功能）**
1. 配置 Cloudflare 生产环境的 4 个变量并重新部署，用第三节的 curl 命令验证返回 200。

**可选优化（按性价比排序）**
2. 接入真实浏览器定位（`navigator.geolocation` + 高德逆地理编码），替换当前固定回填的演示定位——这是交互路径第 1 步唯一未做实的地方。
3. 线上 AI 不可用时的前端兜底：当前会显示服务端错误文案，可考虑降级为"仅按真实天气+POI 排序"的无 AI 路线，保证面试官任何时候打开都有完整体验。
4. 区域选项目前硬编码为上海三个区，切换城市后不匹配，可改为按城市动态生成。
5. 成本字段：高德 `biz_ext.cost` 多数为空，导致预算显示 ¥0。建议显式标注"暂无价格数据"而非展示 0，避免误导。
